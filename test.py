import numpy as np
output_dim = 10
y_target = np.array([[0] * output_dim,
                     [1] * output_dim,
                     [1] * output_dim,
                     [0] * output_dim])

print(y_target)
a = ['a']
del a[0]
print(a)
